// Atom: Chip — small rounded tag for tech / keywords.
export default function Chip({ children }) {
  return <span className="text-xs px-3 py-[5px] rounded-full bg-bg2 text-body">{children}</span>;
}
